import React from 'react';

import './pate-title.css';

/* eslint-disable-next-line */
export interface PateTitleProps {}

export const PateTitle = (props: PateTitleProps) => {
  return (
    <div>
      <h1>Welcome to pate-title component!</h1>
    </div>
  );
};

export default PateTitle;
